#include <iostream>

using namespace std;

int main(){
    bool alphabet[26] = {false};
    int num;
    string input;
    cin>>num;
    for(int i = 0 ; i < num; i++)
        cin>>input;
    for(int i = 0; i < input.length(); i++){

    }
}